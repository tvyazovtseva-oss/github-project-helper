

# План: Ребрендинг «Аннамама», онбординг, навигация, очистка главной

## 1. Глобальный ребрендинг → «Аннамама»

**Файлы:** все 14 файлов с упоминаниями «Anna MAMA» / «Anna mama», + `index.html`

- Заменить все строки «Anna MAMA», «Клуб Anna MAMA», «Anna MAMA» → «Аннамама», «Клуб Аннамама»
- `index.html`: title, og:title, description → «Аннамама — платформа для мам»
- Favicon: обновить букву «A» → «А» в SVG data-URI (если ещё не кириллица)

## 2. Логотип на /login и /register

- Заменить иконку `<Heart>` на текстовый логотип «Аннамама» — крупный, розовый, шрифтом проекта (font-bold text-3xl text-brand-500)
- Убрать подпись «Здоровье и развитие малыша» или оставить как subtitle
- На `/login` — **удалить** весь блок демо-доступа (строки 83-96 в LoginPage.tsx)

## 3. Онбординг после регистрации

**Файл:** `RegisterPage.tsx`

- После `register()` — вместо прямого `navigate('/mama')` показать модальное окно (Dialog)
- Содержимое: анимированная иконка ✦ с pulse + текст «Добро пожаловать в экосистему Аннамама!»
- CSS-анимация: scale-in + fade-in (уже есть в tailwind.config)
- Через 2.5 секунды — автоматический `navigate('/mama')` с плавным fade-out

## 4. Реорганизация левого меню (MamaLayout.tsx)

**Текущая навигация:** Главная, Курсы, Медкарта (center), Знания, Профиль

**Новая навигация (desktop sidebar + mobile bottom):**
- Убрать product switcher из верха sidebar → заменить на логотип «Аннамама» + кнопка «Главная» (всегда `/mama`)
- Навигация: **Главная**, **Курсы**, **Медкарта**, **Знания** (без Профиля)
- Низ sidebar: аватарка + имя пользователя (клик → `/mama/profile`) + иконка `LogOut` (выход)
- Mobile bottom nav: убрать «Профиль», оставить 4 пункта (Главная, Курсы, Медкарта, Знания)
- Product picker (popup) — убрать кнопку «Новые продукты»

## 5. Очистка MamaHomePage

**Удалить:**
- Блок «Данные ребенка» (baby profile card, строки 77-98)
- Блок «Медкарта» (health summary accordion, строки 100-126)

**Оставить:**
- Персональный баннер (hero)
- «Мои продукты» (без кнопки «Новые продукты» — она в picker)
- Лента обновлений

## Технические детали

| Действие | Файлы |
|---|---|
| Ребрендинг текстов | 14 файлов (search-replace «Anna MAMA» → «Аннамама») |
| Логотип + убрать демо | `LoginPage.tsx`, `RegisterPage.tsx` |
| Онбординг popup | `RegisterPage.tsx` (добавить Dialog + анимацию) |
| Навигация sidebar | `MamaLayout.tsx` (переработать sidebar + bottom nav) |
| Очистка главной | `MamaHomePage.tsx` (удалить 2 блока) |
| SEO | `index.html` |

Итого: ~16 файлов затронуто, 0 новых файлов.

